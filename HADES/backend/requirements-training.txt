# Optional HADES training stack.
# This file is intentionally NOT included by backend/requirements.txt so normal
# HADES startup stays lightweight and independent from PyTorch/CUDA.
#
# For NVIDIA acceleration on Windows, install the PyTorch build appropriate for
# the machine/driver first when the default PyPI wheel is not the desired build.
torch>=2.7
transformers==5.17.0
datasets==5.0.1
peft==0.20.0
accelerate==1.15.0

# Optional QLoRA / 4-bit support. Install separately only when the local
# bitsandbytes build supports the machine:
# bitsandbytes

/** @deprecated Prefer `../config/research` — kept for ResearchMockPage decorative demos only. */
export {
  RD_CONTEXT_CHIPS,
  RD_HERO,
  RD_IDLE_TIMELINE,
  RD_INPUT_TABS,
  RD_TEMPLATES,
  type RdEvidenceItem,
  type RdInputTab,
  type RdInsight,
  type RdTimelineStep,
  type RdWebResult,
} from "../config/research";

/** Decorative mock-only rows — NEVER import into live ResearchPage. */
export const RD_MODELS = ["(use live Models API)"] as const;

export const RD_DEMO_EVIDENCE = [] as const;
export const RD_DEMO_WEB = [] as const;
export const RD_DEMO_INSIGHTS = [] as const;
export const RD_DEMO_TIMELINE = [] as const;
